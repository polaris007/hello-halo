# 国际化与开发规范

## 目录
- [语言规范](#语言规范)
- [国际化指南](#国际化指南)
- [代码示例](#代码示例)
- [工具与工作流](#工具与工作流)
- [常见问题](#常见问题)
- [最佳实践](#最佳实践)

## 语言规范

**使用中文进行交流。** 所有对话、注释、文档均使用中文。

## 国际化指南

### 基本规则

- **No hardcoded text.** 使用 `t('English text')` 格式进行国际化
- 不需要编写翻译文件，翻译是自动的
- 提交前运行 `npm run i18n` 命令

### 工作原理

1. `t('English text')` 函数会：
   - 提取英文文本作为翻译键
   - 自动生成对应的中文翻译
   - 在运行时根据用户语言环境返回对应翻译

2. 翻译流程：
   - 开发时使用英文文本作为键
   - 运行 `npm run i18n` 生成翻译文件
   - 系统自动处理翻译映射

## 代码示例

### 正确示例

```tsx
// React 组件
<Button>{t('Save')}</Button>

// 错误消息
const errorMessage = t('Invalid email format')

// 复杂场景
const greeting = t('Hello {name}', { name: userName })
```

### 错误示例

```tsx
// 错误：硬编码文本
<Button>保存</Button>

// 错误：直接使用中文作为键
<Button>{t('保存')}</Button>
```

### 不同文件类型中的使用

**TypeScript 文件：**
```typescript
import { t } from '../i18n'

function getWelcomeMessage() {
  return t('Welcome to the application')
}
```

**React 组件：**
```tsx
import { t } from '../i18n'

function UserProfile({ user }) {
  return (
    <div>
      <h1>{t('User Profile')}</h1>
      <p>{t('Welcome back, {name}', { name: user.name })}</p>
    </div>
  )
}
```

## 工具与工作流

### 命令说明

- **`npm run i18n`**：
  - 提取所有 `t('...')` 调用中的文本
  - 生成或更新翻译文件
  - 验证翻译完整性

### 推荐工具

- **i18n-extract**：提取国际化文本
- **i18next**：国际化框架
- **react-i18next**：React 国际化组件

## 常见问题

### Q: 为什么我的翻译没有生效？
**A:** 请检查：
1. 是否使用了 `t('English text')` 格式
2. 是否运行了 `npm run i18n` 命令
3. 翻译键是否正确（应为英文）

### Q: 如何处理复数形式？
**A:** 使用带参数的翻译：
```tsx
t('You have {count} message', { count: messageCount })
```

### Q: 如何处理性别相关的翻译？
**A:** 使用上下文参数：
```tsx
t('Welcome {name}', { name: userName, context: userGender })
```

## 最佳实践

1. **保持翻译键简洁**：使用简短、描述性的英文文本
2. **一致性**：相同的功能使用相同的翻译键
3. **避免复杂句子**：将长句子拆分为多个短句
4. **测试**：在不同语言环境下测试应用
5. **定期更新**：随着功能变化及时更新翻译
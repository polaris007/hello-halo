## 1. 配置模块改造

- [x] 1.1 修改 `config.service.ts` 默认数据目录从 `~/.halo` 改为 `{cwd}/data`
- [x] 1.2 新增 `resolveDataDir()` 函数实现优先级解析（启动参数 > 环境变量 > 配置文件 > 默认值）
- [x] 1.3 更新 `loadConfig()` 配置文件搜索顺序，优先从数据目录内查找
- [x] 1.4 在 `applyEnvOverrides()` 中添加数据目录来源日志输出
- [x] 1.5 添加数据目录不存在时的自动创建逻辑和权限检查

## 2. 启动参数解析

- [x] 2.1 在 `src/server/index.ts` 添加 `parseArgs` 解析 `--data-dir` / `-d` 参数
- [x] 2.2 将解析结果设置到 `HALO_DATA_DIR` 环境变量
- [x] 2.3 添加 `--config` / `-c` 参数支持指定配置文件路径
- [x] 2.4 启动时输出数据目录解析结果日志

## 3. 数据库模块适配

- [x] 3.1 修改 `database.ts` 从配置服务获取数据目录而非硬编码
- [x] 3.2 移除 `database.ts` 中独立的 `HALO_DATA_DIR` 环境变量读取逻辑
- [x] 3.3 更新数据库初始化流程，确保配置服务先初始化
- [x] 3.4 更新 `runMigrations()` 中的空间目录迁移路径逻辑

## 4. 多租户模块适配

- [x] 4.1 更新空间创建逻辑使用 `getConfig().data.basePath` 作为基础路径
- [x] 4.2 确保用户目录结构 `{data-dir}/users/{user_id}/spaces/` 正确创建
- [x] 4.3 更新空间路径验证逻辑适配新目录结构

## 5. 日志模块适配

- [x] 5.1 检查日志服务是否硬编码了日志路径
- [x] 5.2 更新日志目录为 `{data-dir}/logs/`
- [x] 5.3 支持通过 `HALO_LOG_DIR` 环境变量覆盖日志目录

## 6. 迁移工具

- [x] 6.1 创建 `scripts/migrate-data-dir.js` 迁移脚本
- [x] 6.2 实现旧数据目录检测功能（检测 `~/.halo` 是否存在）
- [x] 6.3 实现数据复制功能（数据库、用户目录、配置文件）
- [x] 6.4 生成迁移报告，输出成功/失败详情
- [x] 6.5 启动时检测旧数据目录并输出迁移提示

## 7. 测试

- [x] 7.1 添加配置服务数据目录解析单元测试
- [x] 7.2 添加启动参数解析单元测试
- [x] 7.3 添加数据库模块数据目录适配测试
- [x] 7.4 添加多租户空间路径测试
- [x] 7.5 添加迁移脚本测试

## 8. 文档更新

- [x] 8.1 更新 `openspec/config.yaml` 数据存储描述
- [x] 8.2 创建 Docker 部署指南文档
- [x] 8.3 更新 README 中的部署说明
- [x] 8.4 创建数据迁移指南文档

## 9. 集成验证

- [x] 9.1 验证开发环境启动正常，数据目录为 `{cwd}/data`
- [x] 9.2 验证环境变量 `HALO_DATA_DIR` 配置生效
- [x] 9.3 验证启动参数 `--data-dir` 配置生效
- [x] 9.4 验证相对路径解析正确
- [x] 9.5 验证数据库、空间、日志功能正常
- [x] 9.6 验证迁移脚本功能
